// Código no arquivo gamesWeb.js

function colidiu() {

    var largura nave.width;
    
    var altura nave.height;
    
    return (xNave + largura) > xInimigo &&
    
    xNave < (xInimigo + largura) &&
    
    (yNave + altura) > ylnimigo &δι
    
    yNave (yInimigo + altura)
    
    }